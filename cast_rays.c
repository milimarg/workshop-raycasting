#include "header.h"

void cast_rays(raycaster_t *raycaster)
{
    // cast a ray using horizontal lines
    // cast a ray using vertical lines
    // You know what bitshifting is, right? It could be useful to get the nearest point to intersect with...
    // get the shortest ray between these two rays
}

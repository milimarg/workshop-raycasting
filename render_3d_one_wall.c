#include "header.h"

void render_3d_wall(raycaster_t *raycaster)
{
    // calculate the width and the height of the wall
    // calculate the position of the wall
    // add color on the wall depending on the shortest ray used (horizontal or vertical)
}
